# 三川工方｜計價工具集

這個 repo 存放三川工方的手機用計價工具。每個工具是一個獨立的單檔 HTML，
打開網址即用，不需安裝、不需後端。

---

## 一、專案結構

```
/
├─ CLAUDE.md          ← 本文件（交接規範，動工前先讀）
├─ index.html         ← 超固 SPC 卡扣式地板・快速試算（已完成）
└─ <工具代號>/index.html  ← 之後每個工具一個資料夾
```

規則：
- **一個工具一個資料夾、一個 index.html**，彼此完全獨立。
- 改 A 工具不得動到 B 工具的檔案。
- 資料夾名稱用小寫英文，例如 `bath/`（廁所）、`kitchen/`（廚房）。
- 部署：GitHub Pages（main 分支 / root）。推上去 1–2 分鐘生效。

---

## 二、視覺規範（所有工具共用）

### 色票
| 用途 | 色碼 |
|---|---|
| 紙感底色 | `#f2f0eb` |
| 卡片底 | `#f8f7f4` |
| 墨色文字 | `#23211d` |
| 標題近黑 | `#26241f` |
| 細線 | `#dcd7cd` |
| 赭紅重點 | `#96503a` |
| 陶土（選取狀態） | `#d0785e` |
| 落款褐 | `#7a574a` |
| 內文褐 | `#5a3b32` |
| 金額綠 | `#2f6b4f` |
| 金額綠底 | `#e6efe8` |
| 淡字灰 | `#a09789` |

### 字體
- 中文內文：LXGW WenKai TC（霞鶩文楷）
- 標題：Noto Serif TC，字重 600
- 數字：Outfit，`font-variant-numeric: tabular-nums`
- 落款「三川工方」：Chiron Sung HK 900，`transform: scaleY(.9)`，字距 `.34em`

### 版面慣例
- 手機優先，內容寬度上限 560px。
- 抬頭：`品名 ｜ 類別 ｜ 用途` 一行（細豎線分隔）→ 細分隔線 → 落款「三川工方」→ 英文副標 → 條件註記。
- 每個區塊標題：中文粗體 + 細線 + 右側英文小字（大寫、寬字距）。
- 金額一律綠色 `#2f6b4f`。
- 底部固定計價條：左側綠色直線 + 金額大字 + 明細小字；下方一排膠囊鍵。
- 膠囊鍵樣式：底 `#e7e4dd`、框 `#d9d4ca`、圓角 999px、字重 700、字距 .12em。

---

## 三、工具骨架（每支工具都照這個結構）

1. **設定區塊**（`<script>` 最上方）：所有價格、係數、常數集中在此。
   調價只改這裡，不動邏輯。
2. **輸入區**：數量／面積（可分區加總）、條件選擇（大按鈕或卡片）、加項勾選。
3. **即時計算**：任何輸入變動立即重算，底部計價條同步跳動。
4. **計算結果明細**：逐項列出「品名／算式／金額」，含小計、稅金、合計。
5. **預覽報價單**：滑出全畫面，套用抬頭規範，可複製文字。
6. **分享**：用 html2canvas 把報價單畫成 PNG，透過 `navigator.share` 傳出。
7. **QR 卡片**：顯示本工具網址的品牌版 QR 圖，可分享、複製網址。

### 技術限制備忘
- **iOS 分享必須在點擊當下呼叫** `navigator.share`。若需先產圖，要預先產生並快取，
  否則會被 Safari 靜默擋掉。做法：開啟卡片時背景先產圖；被擋時按鈕改顯示
  「已就緒・再按一次」。
- 不使用 localStorage（在部分嵌入環境會失效），狀態存記憶體即可。
- 外部套件用 CDN：html2canvas 1.4.1、qrcodejs 1.0.0。

---

## 四、已完成工具

### 1. 超固 SPC 卡扣式地板・快速試算（`/index.html`）
對合富國際股份有限公司的**代工計價**。純施工工資、未稅、交通以臺北市起算，
材料由發包單位提供。

**基礎鋪設費 ＝ 厚度基準價 ＋ 格局加價**

厚度基準價（格局等級 1）：
- 5.5–7 mm：600 元/坪
- 7.5–9 mm：650 元/坪
- 9.5–12 mm：700 元/坪

格局加價：
| 等級 | 特徵 | 加價 | 備料損耗 |
|---|---|---|---|
| 1 | 單一空間、大面積、方正 | +0 | 8% |
| 2 | 多空間分區、門洞多 | +50 | 11% |
| 3 | 牆線歪斜、需測角裁切 | +100 | 14% |
| 4 | 柱位多、凹槽多、轉折多 | +150 | 17% |
| 5 | 弧形、曲線、不規則 | +300 | 20% |

同一案場可分區判定等級，不必整間套用最高等級。
損耗規則：等級 1 為 8%，每升一級 +3%。

特殊拼法與現場追加：
- 人字拼加工 +1,200 元/坪（額外損耗由發包單位備料）
- 矽利康收邊 80 元/公尺（30 坪住宅約估 80 公尺）
- 起步條 200 元/處；分隔條 200 元/門洞
- 門片高度修改 1,200 元/片
- 現場搬工 3,000 元/人/工
- 補土及地坪整平：依現場一式報價
- **每案最低出工工資 8,000 元**（搬工、清運、遠程津貼、住宿另計，不抵扣）

相關施工工項（以等級 1 為基本價）：
- 黑色橡膠底料 300 元/坪起
- 防潮布平鋪 100 元/坪起
- 夾板全面平鋪 800 元/坪起
- 架高地板（完成面 20cm 內）3,200 元/坪起
- 施工餘料清運 120 元/坪起，每案最低 2,500 元

跨縣市：
- 遠程津貼 ＝ 交通時數 × 500 元/小時 × 50% × 施工人數（每人每單程）
- 外宿生活津貼 600 元/人/日（含餐）；住宿房費由發包單位安排負擔
- 油資、過路費、停車費實報實銷

材料規格（超固地板 ELVP）：
單片 1220×185×12mm、每箱 6 片、1.34 m²/箱（≈ 0.405 坪）、21 kg/箱。

---

## 五、規劃中的工具

以「工作內容」分類，不以工種分類（因為客戶是照情境發問的）：
- `bath/` 廁所翻新（工料計價）
- `kitchen/` 廚房翻新
- 天花與隔間
- 油漆
- 老屋全室

新增工具時：複製最接近的既有工具當骨架，只換設定區塊與欄位，
視覺與互動邏輯保持一致。

---

## 六、寫作與溝通慣例

- 一律使用繁體中文（台灣用語）。
- 金額格式：`NT$ 12,345`（千分位、無小數）。
- 坪數保留兩位小數；箱數無條件進位。
- 報價單頁尾固定註明計價前提（純施工／未稅／起算地／材料歸屬）。
